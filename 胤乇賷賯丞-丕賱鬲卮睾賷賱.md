# طريقة تشغيل مشروع "منفذ خريطة"

## المتطلبات
- تثبيت **Node.js** (نسخة 22.5 أو أحدث، لأننا بنستخدم قاعدة بيانات SQLite المدمجة داخل Node.js نفسه بدون أي مكتبات خارجية تحتاج تجميع) من الموقع الرسمي:
  https://nodejs.org
  أو عن طريق `pkg install nodejs` في ترمكس.

> ✅ لم نعد نستخدم مكتبة `better-sqlite3` (اللي كانت بتفشل أثناء التثبيت على بعض الأجهزة بسبب الحاجة لتجميع كود C++). الإصدار الحالي يستخدم `node:sqlite` المدمج داخل Node.js، فمفيش أي تثبيت أو تجميع معقد، وده بيحل مشاكل التثبيت اللي كانت بتظهر في ترمكس.

## خطوات التشغيل
1. فك ضغط الملف المضغوط في أي مجلد على جهازك.
2. افتح **Terminal / موجه الأوامر (cmd)** داخل مجلد المشروع (المجلد الذي يحتوي على `server.js` و`package.json`).
3. ثبّت المكتبات المطلوبة بتنفيذ الأمر التالي مرة واحدة فقط:
   ```
   npm install
   ```
4. شغّل السيرفر:
   ```
   npm start
   ```
5. افتح المتصفح على الرابط:
   ```
   http://127.0.0.1:3000
   ```

## بيانات الدخول التجريبية
| الاسم | اسم المستخدم | كلمة المرور | الصلاحية |
|---|---|---|---|
| محمد نبيه | mohamed | 123456 | مدير |
| علي جبله | ali | 123456 | موظف |

> ⚠️ غيّر كلمات المرور دي قبل ما تستخدم النظام فعليًا مع بيانات حقيقية.

## ملاحظات مهمة
- قاعدة البيانات (`data.db`) تُنشأ تلقائيًا داخل مجلد المشروع أول مرة تشغّل فيها السيرفر، ولا تحتاج أي إعداد إضافي.
- لو عندك نسخة قديمة من `data.db` من مشروع سابق، ضعها في نفس مجلد `server.js` قبل التشغيل؛ السيرفر هيضيف الأعمدة والصلاحيات الجديدة تلقائيًا من غير ما يمسح أي بيانات موجودة.
- لإيقاف السيرفر: اضغط `Ctrl + C` داخل نافذة الـ Terminal.
- لتشغيل السيرفر على منفذ (port) مختلف بدل 3000، شغّله هكذا (مثال لتشغيله على المنفذ 4000):
  - على ويندوز (cmd):
    ```
    set PORT=4000 && npm start
    ```
  - على ماك/لينكس:
    ```
    PORT=4000 npm start
    ```

## هيكل الملفات
```
manfaz-kharita/
├── server.js       # السيرفر وقاعدة البيانات والـ API
├── index.html      # واجهة النظام (تُنسخ تلقائيًا داخل public عند الحاجة)
├── package.json    # تعريف المشروع والمكتبات المطلوبة
├── README.md       # نظرة عامة على المشروع ومميزاته
└── data.db         # قاعدة البيانات (تُنشأ تلقائيًا عند أول تشغيل)
```

> ملحوظة: تأكد أن ملف `index.html` موجود داخل مجلد باسم `public` بجوار `server.js` (أي المسار `public/index.html`)، لأن السيرفر يخدم الواجهة من هذا المجلد.
